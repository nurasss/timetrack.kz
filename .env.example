# Copy to .env (gitignored) for server.js/server.vps.js to send email.
# Without this file, email sending is silently disabled.
SMTP_HOST=smtp.timetrack.kz
SMTP_PORT=465
SMTP_USER=no-reply@timetrack.kz
SMTP_PASS=
